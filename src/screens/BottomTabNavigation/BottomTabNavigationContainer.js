import * as React from 'react';
import BottomTabNavigationComponent from './BottomTabNavigationComponent';

export default function BottomTabNavigationContainer() {
  return (
        <BottomTabNavigationComponent/>
  );
}